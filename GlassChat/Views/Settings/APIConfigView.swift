import SwiftUI

/// API 配置：提供商选择 / Endpoint / API Key（Keychain）/ 连接测试。
/// TODO(Phase 4)：完整实现。
struct APIConfigView: View {
    var body: some View {
        Form { Section("API 服务") { Text("Phase 4 实现") } }
            .navigationTitle("API 配置")
    }
}
